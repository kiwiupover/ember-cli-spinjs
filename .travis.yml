---
language: node_js
node_js:
  - "4"

sudo: false
dist: trusty

cache:
  yarn: true

env:
  global:
    - JOBS=1

matrix:
  fast_finish: true
  allow_failures:
    - env: EMBER_TRY_SCENARIO=ember-canary

script:
  - yarn run lint:js
  - yarn run test

after_success:
  - ember surge
